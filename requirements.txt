# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
scipy>=1.11.0
tqdm>=4.65.0
pyyaml>=6.0

# MediaPipe for hand tracking, object detection, pose estimation
mediapipe>=0.10.0

# Google Gemini API for language generation
google-generativeai>=0.7.0

# Grounding DINO for zero-shot object detection
transformers>=4.30.0
torch>=2.0.0

# Data export formats
pandas>=2.0.0
pyarrow>=12.0.0
h5py>=3.9.0

# Optional: visualization
matplotlib>=3.7.0

# LeRobot v2.1 export video compilation
imageio[ffmpeg]

# Retargeting: robot simulation and URDF parsing
pybullet>=3.2.5
urdfpy>=0.0.22
